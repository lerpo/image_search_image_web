import { createSlice } from '@reduxjs/toolkit';


const initialState: {

} = {

}

export const materielSlice = createSlice({
  name: 'materiel',
  initialState,
  reducers: {

  },
  extraReducers(builder) {

  }
});

export default materielSlice.reducer;
